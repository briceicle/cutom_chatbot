version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: "build"
          command: "exit 1"
  notify:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: "notify"
          command: "echo sending notifications"
  no-op:
    type: no-op

workflows:
  workflow:
    jobs:
      - build:
          name: build-1
      - build:
          name: build-2
      - no-op:
          name: no-op-1
          requires: 
            - build-1
      - no-op:
          name: no-op-1
          requires: 
            - build-2
        
