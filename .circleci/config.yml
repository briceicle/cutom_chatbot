version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: "Build"
          command: "echo build"
  test:
    type: no-op

workflows:
  workflow:
    jobs:
      - build
      - test:
          name: test-1
          requires: 
            - build
      - test:
          name: test-2
          requires: 
            - build
