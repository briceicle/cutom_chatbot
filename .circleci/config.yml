version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current

    steps:
      - run:
          name: "Build"
          command: "sleep 1m"
  test:
    docker:
      - image: cimg/base:current

    steps:
      - run:
          name: "Test"
          command: "echo test"

workflows:
  workflow:
    jobs:
      - build
      - test:
          requires: 
            - build: canceled
