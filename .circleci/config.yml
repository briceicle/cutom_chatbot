version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: "build"
          command: "exit 1"
  test:
    type: no-op

workflows:
  workflow:
    jobs:
      - build:
          serial-group: << pipeline.project.slug >>/build-group
