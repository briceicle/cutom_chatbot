version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: "build"
          command: "exit 1"
  build:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: "build"
          command: "exit 1"
  test:
    type: no-op

workflows:
  workflow:
    max_auto_reruns: 1
    jobs:
      - build
