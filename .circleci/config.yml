version: 2.1

jobs:
  deploy:
    docker:
      - image: cimg/base:current

    steps:
      - run:
          name: "Deploy"
          command: "echo deploy"

workflows:
  workflow:
    jobs:
      - hold:
          type: approval
      - deploy:
          requires: 
            - hold
