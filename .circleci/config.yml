version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: "build"
          command: "echo hello world"
  test:
    type: no-op

workflows:
  workflow:
    jobs:
      - build:
          name: build-one
          serial-group: << pipeline.project.slug >>/build-group
      - build:
          name: build-two
          serial-group: << pipeline.project.slug >>/build-group
